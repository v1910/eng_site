import React from "react"

import {Global} from "Global.js";

let global = Global();

export class TestWrapper extends React.Component {
constructor(props) {
  super(props);
  this.state = {showTest: false};
}  

childFunction=(e)=>{
  e.preventDefault();
  this.props.handleClick("Hello From Child1");
}

render
/*
var indents = [];
for (var i = 0; i < this.props.level; i++) {
  indents.push(<span className='indent' key={i}></span>);
}
return indents;
*/
  render() {
    console.log('111global.curentTest=',global.curentTest);
    if(this.state.showTest) {
//    show test      
      return global.curentTest;
    } else {
// erase test      
    }
    
  }

}