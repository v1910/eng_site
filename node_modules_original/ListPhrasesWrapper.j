import React from "react"

import {Global} from "Global.js";

let global = Global();

//Phrases Menu 
export function ListPhrasesWrapper() {
/*  
  let work1, arr=[]; 
  for(let i = 0; i < global.words_files.length; i++){
    work1 = global.words_files[i];
    arr[i] = <div key={work1} id={work1} className="menu_word" fontSize="11px" onClick={() => ShowWordTest({work1})}>{work1}</div>
  }
*/  
  return (<div id="list_phrases_wrapper"> </div>)

} 